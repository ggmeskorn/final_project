class ChatCon {

   final id_msg;
  final types;
  final incoming;
  final outcoming;
  final msg;
  final pathimage;
  final nll;
  final time;
  final username;
  final isGroup;
  final isMe;
  final isreply;
  final pathImage;
  final status_online;

  ChatCon(this.id_msg, this.types, this.incoming, this.outcoming, this.msg, this.pathimage, this.nll, this.time, this.username, this.isGroup, this.isMe, this.isreply, this.pathImage, this.status_online);

}
